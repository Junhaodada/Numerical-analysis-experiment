"""牛顿插值法"""
import numpy as np


def newton(X, Y, x) -> list:
    n = len(X)
    m = len(Y)
    A = np.array([n, n])


if __name__ == "__main__":
    a = np.array([2, 3])
    b = np.arange(2, 5)
    print(a, b)
